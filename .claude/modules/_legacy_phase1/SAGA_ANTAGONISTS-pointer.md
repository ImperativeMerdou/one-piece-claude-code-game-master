# SAGA ANTAGONISTS — pointer file

The authoritative runtime antagonist roster for the Merdo saga lives at:

**`ONE PIECE RESEARCH/MERDO_SAGA_ANTAGONISTS.md`**

That file is loaded into every session via `bible-sources.txt`. This pointer exists to satisfy the auto-load reference in `.claude/commands/dm-continue.md` (line 112-116) without duplicating content. Any module or loader that expects `SAGA_ANTAGONISTS.md` at the campaign-directory path will resolve here and find the canonical location.

**Build-only (not auto-loaded):** `ONE PIECE RESEARCH/MERDO_SAGA_RIVALS_FUTURE.md` holds the Diadem Pirates and future-foes beyond Tessara. Consult during design only.

Do NOT duplicate content into this file.
